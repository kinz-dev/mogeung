[package]
name = "egui-term"
description = "Vendored terminal emulator widget (egui + alacritty_terminal). See VENDORED_FROM."
version.workspace = true
edition.workspace = true
license = "MIT"

[lib]
name = "egui_term"
path = "src/lib.rs"

[dependencies]
egui.workspace = true
alacritty_terminal = "0.25.1"
anyhow.workspace = true
open = "5.3"

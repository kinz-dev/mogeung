{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "What a window is allowed to ask the shell for. Deliberately small: this client's authority comes from the daemon over a socket, not from the OS. `popout-*` is R-B55's detached pane, which is an ordinary client and gets exactly what `main` gets \u2014 no more, and no less, or its terminal cannot open a pty and fails as a black pane.",
  "windows": [
    "main",
    "popout-*"
  ],
  "permissions": [
    "core:default",
    "core:window:allow-set-title",
    "core:window:allow-start-dragging",
    "global-shortcut:default",
    "notification:default",
    "core:window:allow-minimize",
    "core:window:allow-toggle-maximize",
    "core:window:allow-is-maximized",
    "core:window:allow-close",
    "core:window:allow-start-resize-dragging",
    "core:webview:allow-set-webview-zoom",
    "dialog:allow-save",
    "dialog:allow-open",
    "clipboard-manager:allow-write-text"
  ]
}

---
title: Assumption ledger
status: active
updated: 2026-08-28
---

# Assumption ledger

Every belief the product rests on, and whether we have actually checked it.

**This file exists because of a specific failure.** v0.1 was built and thrown
away not because the plan was bad, but because an assumption underneath it — *"we
must spawn agents in order to populate the queue"* — was never written down, and
so was never reviewable. It survived a whole design document, an implementation,
and a commit before contact with reality killed it in one sentence.

## Status values

| Status | Meaning |
|---|---|
| `UNTESTED` | We believe it. We have no evidence. **Build carefully.** |
| `SUPPORTED` | Evidence exists and points our way |
| `AT RISK` | Evidence suggests it may not hold |
| `REFUTED` | Known false. Linked to the decision that responded |

## The rule

No feature spec may be written without a `depends_on:` naming its assumptions.

**If a spec depends on an `UNTESTED` assumption, the work is to test the
assumption — not to build the feature.**

## Ledger

| # | Assumption | Status | Evidence | Resolution |
|---|---|---|---|---|
| A1 | A cross-session attention queue changes where the user looks | `SUPPORTED` | **Settled 2026-08-04** by the week item 0 asked for: reported as carrying 70–80% of all interaction with agents, with the queue as the way in. v0.1 died before this question could even be asked, so this is the first evidence the product has ever had about its own premise. What is still unmeasured is whether the *ranking* is right, as opposed to the queue existing | [feature 0029](../features/0029-desktop-client.md) |
| A2 | mogeung must spawn agents to populate the queue | `REFUTED` | v0.1 use, 2026-07-25: "a handicapped Claude Code with a single session" | [ADR-0003](../decisions/0003-observe-do-not-spawn.md) |
| A3 | Keyword heuristics over diff text are good enough for reading order | `UNTESTED` | Ranked `auth.rs` above a lockfile once, in a test | — |
| A4 | Claude Code's on-disk formats are stable enough to depend on | `AT RISK` | Undocumented private files. 13 event types classified against a 20,648-line corpus; 3 had been silently swallowed. **Re-swept 2026-08-07 against a corpus grown to 315 transcripts, and the format had moved: a fourteenth top-level type, `bridge-session`, was in the newest sixty and in neither list** — classified `Ignored` the same day (it carries a session id, the bridge's id for it, and a sequence number; no content, no tokens, no tool use) and pinned in `types_found_in_the_real_corpus_are_all_classified`. This is the row doing exactly what it says: the format drifted inside two weeks, and the drift was **findable** rather than silent. **Third drift event, 2026-08-20: two more types, `agent-name` (249 lines) and `atis-latch` (384), over a corpus of 275 transcripts and 99,106 lines** — and this time nobody had to read a paragraph to find out, because `R-J28` had turned the hand-written sweep into `--bin sweep`, which **exits non-zero** on an unclassified shape. That is the difference worth recording against this row: the first drift was found by the canary from a running daemon, the second by a human re-running a script, and the third by a command that failed. The dispositions went opposite ways on evidence — `atis-latch` is empty in every one of its 384 lines and is ignored; `agent-name` duplicates a field we already read and is **read anyway**, so that a title survives `ai-title` being retired. Three types in four weeks is also the clearest measurement this row has of the *rate*: the format moves roughly weekly and none of it is announced It stays `AT RISK` and always will — instrumentation does not make a private format stable — but the claim that changes are loud has now been exercised twice. Note the sweep was run **by hand**, because the canary only speaks from a running daemon and none had been up long enough; worth repeating by hand after any CLI upgrade | Canary shipped ([0001](../features/0001-trust-the-tool.md)); drift is now loud, not silent |
| A5 | Content-hash hunk anchors keep review marks stable across rewrites | `SUPPORTED` | Verified live: `auth.rs` stayed read while a rewritten `main.rs` came back unread | — |
| A6 | The user will run 3–4 concurrent sessions in normal work | `SUPPORTED` | **Settled 2026-08-04** alongside A1, by the same week of use. Never measured directly — the evidence is adoption at 70–80%, which is hard to reach on one session at a time — so this is supported by consequence rather than by count. A real measurement would be the queue's own depth over the week, and nothing records it yet | [feature 0029](../features/0029-desktop-client.md) |
| A7 | Reviewing agent output is a distinct activity worth its own tool | `UNTESTED` | Stated in [concept.md](concept.md) §1, never validated | — |
| A8 | Per-session diff attribution by edited files is accurate enough | `AT RISK` | Cannot separate two sessions editing the same file | — |
| A9 | Git is the right diff base for observed sessions | `SUPPORTED` | Works, but the base is HEAD-when-first-seen; sessions predating mogeung diff meaninglessly | — |
| A10 | Doc sprawl is a real and painful problem worth tooling | `UNTESTED` | Stated as the opening complaint; two versions shipped without touching it | — |
| A11 | An **embedded** terminal widget can render Claude Code's TUI well enough to answer a prompt | `SUPPORTED` | Live use, 2026-07-26: the pane renders a real session and takes typed **and dictated** text. Three acceptance items remain unchecked — arrows-and-Enter on a menu, `Shift+Tab` plan mode, `Ctrl+C` — and the three defects that made the pane unusable on first open were all in focus handling, not rendering, so the widget itself is doing better than feared. **Re-answered by a different widget, 2026-08-05**: the vendored egui terminal it was written about was deleted with the egui client ([ADR-0020](../decisions/0020-the-egui-client-is-retired.md)) and xterm.js took over, in daily use since 2026-08-04. The assumption was always about *embedding a TUI at all* rather than about egui, which is why it survives the implementation being replaced rather than needing to be re-tested from scratch | Attached terminal shipped ([ADR-0010](../decisions/0010-attach-a-terminal-never-own-one.md)); see the acceptance list in [feature 0003](../features/0003-attached-terminal.md) |
| A12 | Starting sessions with `yolomo` is a change the user will actually adopt | `SUPPORTED` | **Settled 2026-08-07 by counting, which is what this row asked for.** Every live agent session on the machine was started by `yolomo`: seven tmux sessions, **six of them `mogeung-*`** — the prefix `scripts/yolomo` builds from the working directory — across four projects, spanning two days, five of them attached. One carries the `-2` suffix, so the two-agents-in-one-directory case is in real use rather than theoretical. The seventh is a hand-made `mog-0` and is not an agent. First use was 2026-07-26; twelve days later it is how sessions get started, which is the definition this row was written against. **Two honest limits on that number.** It is a *snapshot*, not the week-long count the row imagined — a habit visible on one afternoon could still lapse. And it counts tmux sessions bearing yolomo's naming rather than sessions the daemon successfully matched to a `tmux_target`, which is the thing the Agent pane actually needs; those can differ if matching fails, and nothing here measures that. What is settled is the *human* half — the habit stuck. The plumbing half is `A11`'s and is separately `SUPPORTED` | — |
| A13 | The user drives by keyboard and will reach for a palette before a menu | `SUPPORTED` | Stated directly — *"ppl using this is often a pro-level user who love to use keyboard to navigate"* — and the keymap system exists because of it. What is still unchecked is whether the palette gets used *after* the bindings are learnt, or is abandoned once it has taught them. Either outcome is a success for the feature | [feature 0005](../features/0005-reachable-by-keyboard.md) |
| A14 | The user wants two detail views at once and will arrange them rather than leaving the default | `SUPPORTED` | Asked for directly: *"put info and debt side by side"*. Wanting a layout is weaker evidence than keeping one, so what is still open is whether the default tab container is ever actually dragged apart in a week of use | [feature 0006](../features/0006-dockable-panes.md) |
| A15 | Reading worktree files inside mogeung — read-only, never editing — is worth a pane | `SUPPORTED` | Asked for directly, 2026-07-27: *"a file explorer tab that will show the file tree at the left and the editor pane at the main screen"*. The ask said "editor"; the roadmap says an editor is permanently out of scope, so what shipped is a viewer — whether reading without editing satisfies the want is exactly what a week of use will show | [feature 0007](../features/0007-file-explorer.md) |
| A16 | The explorer earns enough use to deserve workbench affordances — tabs, search, remembered state | `SUPPORTED` | Asked for twice in one day, 2026-07-27: the enhancement list itself, then — offered a staged plan that would gate the expensive parts on observed use — an explicit *"make it a single pass"*. A deliberate commitment is stronger than a wish, and weaker than use; A15's caveat stands, and the dogfooding week now judges the whole workbench at once rather than in instalments | [feature 0008](../features/0008-explorer-workbench.md) |
| A17 | Hand-applied session labels beat derived titles for triage, and will keep being applied | `SUPPORTED` | Asked for directly, 2026-07-27, badge design included — a specific ask is stronger than a vague one. The failure mode is not week one but week two: labels are cheap to add and cost a hand-edit each to *maintain*, so the honest test is whether any label applied in the dogfooding week is still accurate at the end of it | [feature 0009](../features/0009-session-labels.md) |
| A18 | Commit history, per-commit diffs and blame are worth a pane beyond the Changes tab's session diff | `SUPPORTED` | Asked for directly, 2026-07-27, with features named and IntelliJ's changelist given as the reference — though the presentation was explicitly left open (*"I don't have a good idea how should we show it"*), so the spec carries open questions rather than answers. Weaker than the other asks in exactly that way | [feature 0010](../features/0010-git-view.md) |
| A19 | The git pane earns commercial-grade reading depth — branches, stashes, graph, re-blame — beyond log and status | `SUPPORTED` | Asked for as *"on-par with a commercial product"*, then — offered priority tiers — an explicit *"build P1 & P2 in one-go"*, 2026-07-28. The A16 shape: a deliberate one-go commitment, stronger than a wish, weaker than use. The dogfooding week judges each section separately; an unused section is a removal candidate, and the read-only fence is restated in the spec precisely because this is where "just add staging" pressure will arrive | [feature 0011](../features/0011-git-depth.md) |
| A20 | Limit-hit evidence in transcripts suffices to warn about the five-hour window | `AT RISK` | 2026-07-29 sweep of all 235 local transcripts: **no structured limit event exists on disk** — the premise `R-G1` was written on — only a synthetic assistant line (`model: "<synthetic>"`, *"You've hit your session limit · resets 8pm"*; 3 found). No quota telemetry at all, so any pre-exhaustion warning is an estimate from burn history and must say so. **Re-swept 2026-08-07 over 315 transcripts: the conclusion holds and the sample is seven times larger** — 21 files now carry the synthetic line, and a search for any structured `limit`-shaped event type still returns nothing. Twelve days and a CLI version later, the estimate is still an estimate, which is the thing this row exists to keep honest | — |
| A21 | Transcripts carry enough evidence to check an agent's claims — tests ran, edits verified | `SUPPORTED` | `tool_use`/`tool_result` pairs are fully parsed already; "did a Bash call actually run the tests" is answerable from data on disk (17,849 assistant lines in the local corpus). Whether seeing the check changes behaviour is A7's question, not this one's | — |
| A22 | Cross-session mining — transcripts plus `history.jsonl` — yields triage-worthy signal | `UNTESTED` | The material exists (2,076 prompts, 471 sessions, 54 projects on disk) and nobody has ever looked at it crosswise. Built at the 2026-07-29 *"finish the R-\* items in one-go"* ask (A16/A19 shape); the dogfooding week judges | — |
| A23 | The Session model generalises beyond Claude Code — Codex first | `UNTESTED` | Building the adapter is the stated test (`R-I1`). Local `~/.codex` is a fresh install: the schema is real and readable (`state_5.sqlite`, 42 migrations) but holds zero sessions, so structure is verifiable and end-to-end mapping is not until Codex sees real use. **A third adapter, 2026-08-25 (`R-I15`, Qwen Code), moved this further than Codex could** — and the distinction matters, because `~/.qwen` holds **real sessions** where `~/.codex` holds none, so this is the first time the claim has been put against actual data rather than a schema. The structural half now has an answer and it is *yes*: absorbing a third CLI added **no field to `Session`**, and the git observer, diff base, attention ranking, queue, snooze, labels and tags all worked on a Qwen session the first time they saw one. What generalised was the `Session`, not the parser — the three readers share only `LineClass`, which [ADR-0029](../decisions/0029-an-agent-cli-is-a-variant-not-a-plugin.md) decided to keep rather than fix. **Two caveats keep this `UNTESTED` rather than flipping it.** (a) The end-to-end half is unrun: no working day has yet passed with Qwen and Claude sessions tiering in one queue, which is the only thing that tests the *ranking*, as opposed to the model. (b) One thing did **not** generalise, and it is the product's core question: Qwen persists no busy/idle state, so a session blocked on a tool approval and one running that tool are indistinguishable on disk. `Session` has a `live_status` field to put an answer in; Qwen has no answer to put there. That is a limit of the *source*, not of the model — but a row claiming the model generalises should say where the model is fine and the world is not | Structural half answered by `R-I15` ([0036](../features/0036-qwen-code-adapter.md)); end-to-end half still open |
| A24 | A read-only daemon is safe to reach over a trusted network with a shared token, without TLS | `UNTESTED` | `--token` is enforced on HTTP and WS (`R-I4`) and, since 2026-07-31, is **mandatory** beyond loopback — a public bind without one refuses to start rather than warning (`R-I10`). So the assumption is now the thing actually being tested, rather than a bet nobody was held to. There is still no TLS. **The word "read-only" in this row is load-bearing**, and [ADR-0012](../decisions/0012-write-locally-never-publish.md) is about to make the daemon capable of writing a repository — which is why that ADR refuses write verbs unless the bind is loopback or a token was presented. Without that guard the feature would void this assumption rather than depend on it. TLS stays out until the assumption fails | — |
| A25 | A glanceable waiting count outliving the window earns its keep against notifications | `UNTESTED` | The roadmap itself flags `R-C1` banners as possibly making `R-C2` redundant, and left it open deliberately. Built at the one-go ask; if unglanced in the week it is a removal candidate | — |
| A26 | The user will commit from mogeung rather than from the terminal tab open beside it | `UNTESTED` | Asked for directly 2026-07-30 — *"Do we support git commit and push in the GIT UI now?"* — and, offered four tiers, local-writes chosen over the cheaper compose-the-command path. A want, and a deliberate one, but the competitor is unusually strong: [feature 0024](../features/0024-in-app-terminal.md) put a real shell one keystroke away and it already knows git. The failure mode is not week one but a build that widened the product for nothing, so the removal condition is written before the code: unused through a dogfooding week and the verbs come out, replaced by the composed-command alternative ADR-0012 kept as its fallback | [ADR-0012](../decisions/0012-write-locally-never-publish.md); [feature 0025](../features/0025-git-write-local.md) |
| A27 | The user will write notes inside mogeung rather than in the editor already open beside it | `AT RISK` | **2026-08-05, first count: two real notes in a week** (four files, two of them probes from the build), 99 and 114 bytes. That is below the removal condition agreed in advance, so this is evidence *against*. The condition was **not** executed: the gate came off on an explicit decision the same day, with the reasoning recorded in [feature 0026](../features/0026-notes-and-tasks.md) — one of the two notes is called `todo-list`, which is `R-L3` asking for itself in the wrong container, and the slice being measured (a remark bound to a turn) is not the thing that was asked for (a scratchpad that outlives the session). What ships instead is the copy-a-conversation-into-a-note gesture that was missing, on the argument that an empty box competes badly with an editor and a loaded one may not. **`R-L2` was closed `kept` on 2026-08-07 and this row did not move with it** — deliberately, and it is the mistake the 2026-07-29 backlog pass made: a feature row and the assumption under it are different questions, and closing the first does not answer the second. The condition below is about a **month**. **The replacement condition:** a month of notes that were only ever created empty means the copy gesture went unused and pillar L comes out on that evidence rather than on a file count. Asked for directly 2026-08-02 (*"Tasks / Scratch pad concept"*) and designed the same day. The same shape as A26 and a harder bet: the competing tool is not a terminal but an editor that is already open, already has your keybindings, and already holds the file you are talking about. Tested deliberately at the smallest scale first — a note against a transcript turn (`R-B35`) is the one place a note has an obvious home and nothing is closer to hand. **Removal condition agreed in advance:** fewer than a handful of notes in a week and it comes out rather than being decorated | [ADR-0015](../decisions/0015-markdown-is-the-truth.md), [feature 0026](../features/0026-notes-and-tasks.md) |
| A28 | A file tree visible beside every tab is worth 30px of permanent chrome | `SUPPORTED` | **Settled 2026-08-05** by the week the removal condition asked for: the rail was not left collapsed, so Files stays where `R-B41` put it and the rail keeps its 30px. The evidence is one report covering *about 90%* of that backlog rather than a measurement of how often the tree was used, so this is the weakest `SUPPORTED` in the ledger — enough to stop the removal clock, not enough to claim the tree earns the chrome on any day in particular. Asked for 2026-08-03 with four screenshots of RustRover, which is a specific ask and therefore stronger than a wish. Weaker than it looks in one way: what was shown is what the user's *other* editor does, not something they have missed in mogeung and said so. Cheap to test — the tree already exists inside the Editor tab and **moves** rather than being written, so the cost of the experiment is a panel and a strip. **Removal condition agreed in advance:** the rail collapsed through a week of use and Files goes back into the Editor, taking the rail with it | [ADR-0017](../decisions/0017-the-rail-is-chrome.md), [feature 0027](../features/0027-right-rail.md) |
| A29 | One search box over three corpora beats three separate searches | `SUPPORTED` | **Settled 2026-08-05** by the same week and the same report as [A28](#ledger): the panel was reached for rather than abandoned to the palette, so the removal condition below does not fire and both boxes stay. Same weakness as A28 — a kept-it verdict, not a count of which box was opened. Asked for 2026-08-03. A bet about attention, not capability: `R-B36`, the palette's content search and `R-F1` already exist and already work, and nothing federated can find anything the three could not. What is untested is whether choosing between them is the actual cost — and the competing tool here is our own palette, which is fast, keyboard-driven and already learnt. **Removal condition agreed in advance:** if the palette stays what gets reached for, the panel comes out and the three boxes stay separate | [feature 0028](../features/0028-global-search.md) |
| A30 | The user will keep two agent sessions on screen at once, and arrange them, rather than returning to one | `SUPPORTED` | Asked for directly 2026-08-06 — *"show two Agent panel side-by-side"* — with the reason attached: the Agent pane follows the selection, so answering one waiting session hides the other. A specific ask, and therefore stronger than a wish; the A14 caveat applies unchanged and matters more here, because wanting a layout is weaker evidence than keeping one. **Deliberately not folded into A14**, which reads *two detail views at once* and was settled by putting Info beside Debt: that is two views of one session, and this is one view of two sessions. The docking machinery is shared and the bet is not, so a favourable verdict on A14 says nothing about this. The uncomfortable direction is toward [A1](#ledger): the queue's whole claim is that it *tells* you which session needs you, and a user who keeps two agents visible has started watching instead of being told. That is not a reason to refuse the row — it is what makes the verdict worth reading. **Removal condition agreed in advance:** a week in which a second Agent pane is opened and closed again the same session, rather than kept, and holding comes out with it. **Kept 2026-08-07, and the verdict is thinner than the row's own condition.** `R-B49` was closed `kept` in the 2026-08-07 backlog pass after a day of real use — the removal condition asked whether a second Agent pane is *kept* rather than opened and closed, and a day cannot answer a question about habit. Filed as `SUPPORTED` on the strength of the arrangement being used and extended (spatial pane navigation, `R-B52`, was asked for *because* two panes existed), and flagged here so nobody later reads the ✅ as a week of evidence. **First run 2026-08-06, reported as working better than expected** — recorded because it closes what the tests could not reach (two live attaches side by side, and the focus ring being visible at all), and deliberately *not* recorded as a verdict: this row is a bet about the second week, and an hour of it going well is the weakest possible evidence for a claim about whether a layout gets **kept** | [feature 0030](../features/0030-two-agents-at-once.md) |

| A31 | A dollar figure computed from a price sheet we maintain is worth more than the tokens it is computed from | `UNTESTED` | Asked for directly 2026-08-08 — *"how much do I spend today and historical… a Opus or Fables 5 model cost a lot more than an older / smaller model"* — and the ask names the reason the tokens stopped answering it: this machine's corpus runs five models whose per-token prices span 10×, so one token count cannot say where the money goes. A specific ask, and therefore stronger than a wish. **The bet is not that the number is useful; it is that it is worth the maintenance.** [ADR-0005](../decisions/0005-tokens-not-dollars.md) refused a price table on the grounds that it goes stale and that keeping it current is not this product's job, and [ADR-0024](../decisions/0024-equivalent-cost-in-dollars.md) overturns that without refuting it — the staleness is now ours, answered by an as-of date, per-day rates, and an unpriced-not-free rule rather than by anything that checks. The per-model **token** split underneath is not part of this bet and stays whatever the verdict: it is measured, not quoted. **Removal condition agreed in advance:** the table found stale in the wild, or a month in which the dollar figure is looked at and the per-model token split is what actually gets used — either way the dollars come out and the split stays | [ADR-0024](../decisions/0024-equivalent-cost-in-dollars.md); [feature 0033](../features/0033-cost-per-model.md) |

| A32 | Reading the IDEs' own run configurations gives us enough to run, without a format of our own | `AT RISK` | **Measured the day it was written, 2026-08-09, across all 19 git repositories on this machine** — and the measurement is why this row is `AT RISK` on arrival rather than `UNTESTED`. `.vscode/launch.json`: **1**, and it is `github/codex`, a third-party clone rather than the user's work. `.vscode/tasks.json`: **0**. `.idea/runConfigurations/*.xml`: **0**. `.run/*.run.xml`: **1** repo, 4 configurations, Java/Spring. `.idea/workspace.xml` carrying a `RunManager` component: **10** — the majority, and that file is per-user and conventionally gitignored, which is where IntelliJ puts a configuration you made and never ticked *store as project file*. **mogeung's own repository has none of them**, so a feature built strictly to this assumption would be inert in the repository where it is developed. The response is in [ADR-0026](../decisions/0026-other-peoples-run-configurations.md): read all four sources, put the gitignored one third rather than last because that is where the evidence says they are, and make **detection a first-class source rather than a fallback**. What would move this to `SUPPORTED` is the sweep (`R-N1`) finding, on a second machine or a later day, that configurations exist for the projects actually worked in. What would `REFUTE` it is the same sweep finding that the runs people want are ones no file describes — in which case detection is not a supporting act, it is the whole feature, and the parsing half should be dropped rather than maintained. **Later the same day, the scope was cut** — *"may be just drop the intellij's runConfiguration and the Java support for now"* — and the response was to take the refutation branch **pre-emptively rather than wait for it**: counted by *files* instead of by *IDEs*, the table above is five checked-in configurations across nineteen repositories, so the majority case is a project with no shared run configuration at all and the IDE split is a detail. [ADR-0026](../decisions/0026-other-peoples-run-configurations.md) therefore makes detection the source and `launch.json` a bonus, and `R-N12` defers IntelliJ with the condition to take it up. **This row stays `AT RISK` rather than being marked `REFUTED`**, and the distinction is real: nothing has shown that reading configurations is *worthless*, only that a feature resting its weight on it would have fallen over. The status to watch for is the opposite of the usual one — if `R-N2` turns out to carry real traffic, this row was truer than the measurement made it look. **Re-measured 2026-08-11 by `R-N1`, on the second machine this row named** — Linux, **58** repositories — and it splits cleanly in two directions, which is why the status does not move. *Against* the assumption, and more strongly than before: `.vscode/launch.json` is **1** of 58 and still `github/codex` rather than the user's own work, `tasks.json` is still **0**, and **35 of 58 (60%) carry no configuration of any kind**. *For* it, and unexpectedly: `.idea/runConfigurations/*.xml` is **5** where the Mac had 0, `.run/*.run.xml` is **8** where it had 1, and those thirteen are the user's own projects rather than clones — 14 of 58 (24%) carry a checked-in configuration against 2 of 19 (11%) on the Mac. So the SUPPORTED condition above — *"configurations exist for the projects actually worked in"* — is **met in the one format ADR-0026 deferred**, and met in none of the formats it reads. That is not this row moving; it is `R-N12`'s trigger, arriving as a measurement rather than a memory, which is what ADR-0026 said the sweep existed to produce. The honest summary is that the *decision* was right and the *evidence* for it was one machine: detection still has to carry 60% of repositories alone, and IntelliJ is where the written-down configurations actually are. **Re-measured again 2026-08-20, and this time by configurations rather than by files: 243 checked-in IntelliJ configurations across 13 of 59 repositories**, all in the user's own projects, against one `launch.json` belonging to a third-party clone. That fired `R-N12`'s trigger and produced [ADR-0028](../decisions/0028-intellij-when-there-is-a-debugger.md) — but the same sweep is also **the sharpest evidence yet against the words in this row**. *"Enough to run"* is what is claimed, and **not one of the 243 names a command that can be spawned**: 194 are a Java main class plus an IntelliJ module whose classpath IntelliJ resolves from its own project model, and 49 are composites of those. So the format carries the **identity** of a run — what it is called, what it starts, in which module — and not the **means** to start it. That is a different failure from the one this row has been tracking: the files are not scarce any more, they are insufficient on their own. It stays `AT RISK` rather than moving to `REFUTED` because the missing half is known and has a named answer (attach over JDWP, `R-N9`), not because the evidence is thin |

| A33 | The user will run and debug inside mogeung rather than switching to the IDE already open beside it | `UNTESTED` | Asked for directly 2026-08-09 — *"I am thinking about add a 'Run and Debug' feature in mogeung. To make a real IDE like environment"* — with a screenshot of VS Code's own panel. A specific ask, and therefore stronger than a wish; the A14 caveat applies and matters more here than anywhere it has been applied before, because **the competition is excellent and one keystroke away**. A27 is the cautionary precedent: notes were asked for just as directly and are `AT RISK` because the editor beside mogeung turned out to be where writing already happened. Running has the same shape and a stronger incumbent — IntelliJ's debugger is not a text box. **The removal condition is written before the code, deliberately**: if a fortnight of use shows runs started in mogeung and then repeated in the IDE, the panel comes out rather than being improved, because "it is nearly as good as RustRover" is a losing position to defend forever. A34 is the reason to think it might hold, and it is a different claim |

| A34 | Verification is the human's remaining job, so the run belongs beside the claim rather than in a separate tool | `UNTESTED` | This is the argument [ADR-0025](../decisions/0025-run-a-process-you-named-never-an-agent.md) rests on, and it is a **narrowing** of A33 rather than a restatement: A33 says you will run things here, this says the *reason* is that the thing you want to run is the one the agent just claimed passed. [ADR-0019](../decisions/0019-a-viewer-not-an-editor.md) settled the editor question by working out what the human still does — *"the agent does the editing, and what the human does is read"* — and this row says that was right and incomplete. The partial support is that the product already built three quarters of the loop and stopped: `R-E1` records the build and test commands a session ran, `R-E3` binds *"tests pass"*-shaped prose to that evidence or visibly to none, and neither can answer *did it really?* **What distinguishes it from A33 as evidence**: A33 is satisfied by a run panel anyone would use; this is satisfied only if the runs that get started are the ones anchored to a claim. If runs are started from the file tree and never from a session's row, A33 may hold while this is refuted — and that would mean mogeung had built a general-purpose run panel, which is the thing pillar K exists to refuse |

| A35 | A local model's reading of mogeung's own evidence is worth the screen space it takes | `UNTESTED` | Asked for directly 2026-08-27 — *"I have a local running AI model that I can use and call as API"* — and a specific ask is stronger than a wish, but this row has **no evidence at all** and should not be read as though the ask were any. It carries the whole of pillar `O`, which is why `R-O2` is a harness rather than a panel: the doc rule is not optional and the pillar's first work is to test this. What makes it genuinely uncertain is that the competition is not *nothing* — it is the keyword heuristics that already ship, which are fast, explainable and free, and a model that is merely *as good* loses to them on every axis that is not quality. **Removal condition agreed in advance:** if `R-O2` shows the model's ordering agreeing with the keyword ordering where it matters, or disagreeing and being wrong, the pillar comes out rather than being tuned — "a better prompt would fix it" is the losing position [A33](#ledger) also has to defend | [feature 0038](../features/0038-a-local-model.md); [ADR-0030](../decisions/0030-a-model-reads-the-evidence.md) |

| A36 | The rationale for a change is in the transcript that produced it, and is reachable from the line | `UNTESTED` | The bet under `R-O4`, and the one place this product could answer a question nothing else can: a review tool has the diff, an IDE has the code, and only mogeung has the conversation beside them. **The structure is not the bet** — `R-F2` links a file to the sessions that touched it, `R-F9` and `insight::turns_near` link a moment to its turns, and all of it works today. The bet is that the turns near a line actually contain the **why**. The reason to doubt it is specific and worth writing down before it is discovered: an assistant narrates *what it did* far more often than *why*, and the why is usually in the human's prompt several turns earlier — so the retrieval may be pointing at the wrong end of the conversation, which is a fixable design error masquerading as a failed assumption. **Removal condition agreed in advance:** a fortnight in which the answers cite turns that do not contain the reason; the honest response is to try prompts-not-replies once before removing, and to record that as the second attempt rather than as the first working **First evidence, 2026-08-29, from `--bin why`** — the harness that gates `R-O4`, built before the panel on `R-O2`'s precedent. Over 14 edit moments from this machine's sessions, asked twice: **a reason was found in 5 of 14** either way, so *not in these turns* is the majority outcome and `R-O4` must render it as an answer rather than as a failure. The sharper finding is the one this row predicted in writing: **the retrieval was pointing at the wrong end of the conversation.** `R-F9`'s nearest-in-time turns — what `R-O4` would have inherited by default — cited 1 human turn against 12 of the assistant's, and **4 of its 5 answers rested on the assistant's narration alone** (*the file was changed because the assistant then wrote the file* is a sentence, not a rationale). The `leading-up` shape — the human prompt at or before the edit, and everything between — cited 5 human turns and produced **no** narration-only answer. So the doubt written into this row was a **fixable design error**, and it was found before an L-sized panel was built on top of it. The status stays `UNTESTED`: a harness says the reason is reachable when it is there, not that the answers are worth the screen, which is what the fortnight `R-O8` owns is for | [feature 0038](../features/0038-a-local-model.md) |

| A37 | A chat panel inside mogeung gets used rather than the assistant already open beside it | `UNTESTED` | Asked for directly 2026-08-27 — *"a generic AI chat panel at the right panel where I can ask random quick questions"* — and it is **[A27](#ledger)'s shape exactly**, which is the most useful thing to know about it. A27 said the user would write notes in mogeung rather than in the editor already open beside it; it is `AT RISK` on a count of two notes in a week, because the incumbent was already open and already better at the job. The incumbent here is a chat window, competing with a chat panel, and it is a stronger incumbent than a text editor was. What argues *for* the row is the only thing that distinguishes it: the question is asked while looking at something in mogeung, and the cost A27 lost to was the switch rather than the writing. `R-O5` is therefore built to be cheap to remove — it stores nothing, and its persistence is `R-L2`'s copy-into-a-note. **Removal condition agreed in advance:** a fortnight in which the panel is opened and abandoned, or in which the questions asked in it get re-asked elsewhere — the panel comes out and nothing replaces it. **Day one, 2026-08-28:** used for real questions against both the local box and a subscription-backed model, and **four refinements were asked for from use** — a conversation history (`R-O9`), streaming (`R-O11`), a better name and chord (`R-J80`) and focus on open (`R-J82`). Nobody asks for a history in something they have stopped opening, so this is the best evidence available this early and it points the right way. It is still **not the test**: `A27` looked fine on its first day too and went `AT RISK` on a count of two notes in a week, and the incumbent here is stronger. Stays `UNTESTED` until the fortnight `R-O8` owns has actually passed | [feature 0038](../features/0038-a-local-model.md) |

| A38 | Embeddings find what substring search missed, often enough to earn a second list | `UNTESTED` | [Feature 0017](../features/0017-cross-session.md) put semantic search out of scope with a reason and a sequence — *"Semantic/embedding search — honest substring/token search first"* — so this row exists because that condition is met rather than because the refusal was wrong. **It is two claims and they can take different verdicts**, which is the [A33](#ledger)/[A34](#ledger) distinction repeated: the *search* half asks whether a second list beside grep gets read, and the `R-F4` half asks whether recurring failures cluster better by meaning than by literal text. The second is the one with a concrete failure to point at — a failure worded freshly each time is invisible today — and it may hold while the first is refuted, in which case the embeddings stay and the second list goes. **Removal condition agreed in advance:** `R-O2`'s recall run showing the semantic list adding little grep did not already find, or a fortnight in which it is the list that gets ignored — either way the second list comes out and grep stays alone, which is where feature 0017 deliberately started **First evidence, 2026-08-29, from `--bin judge --recall`** — the harness `R-O6` is gated on, run over 337 lines of this machine's corpus with `bge-m3`. The ground truth is generated rather than curated: the model paraphrases a real corpus line into a query sharing none of its distinctive words, and both engines are asked to find the original. **The search half of this row is supported, and the shape of the support is the finding.** On paraphrases the semantic list found **7 of 11** where grep found **0** — which is by construction, since a paraphrase shares no substring, and is why the harness also asks the reverse question. On **literal** queries sliced out of the corpus, grep found **10 of 11** and the index found **5**: it ranked away half of what grep already had. So the two lists are **complementary and neither dominates**, which is precisely the row's own design — *labelled `similar`, beside the grep results and never instead of them* — and is a refutation of any version that replaces search rather than extending it. The `R-F4` half (do recurring failures cluster better by meaning than by literal text?) is **still untested**; it is the half with a concrete failure to point at, and it takes its own verdict **The `R-F4` half was measured the same day, by `--bin judge --clusters`, and it is supported too.** Over **232 literal failure groups** from this machine: at a 0.85 threshold, 20 clusters join 32 rows, and the joins are the ones no normalisation can make — **nine spellings of one shell error** (`(eval):1: == not found`, `unmatched '`, `==codex.rs not found`), five of a browser timeout, four of a two-minute command timeout. They share not one distinctive word, so the literal list files each as its own failure and a fresh wording makes a fresh row, which is exactly the invisibility this half of the row claimed. **The threshold is the finding, not a setting**: at 0.75 the *zsh rejected my command* family swallows two different mistakes (a bad glob and an unquoted `==`), and at 0.92 almost nothing joins and the feature is a rename of the list that exists. Both halves of A38 are therefore **supported by measurement**; what neither harness can answer is whether the second list and the second grouping get **read**, which is the fortnight `R-O8` owns | [feature 0038](../features/0038-a-local-model.md) |
| A39 | A send button stays a button, and does not become the way sessions are driven | `UNTESTED` | The bet under [`R-B54`](roadmap.md#b-sharpen-the-queue) and under [ADR-0003's 2026-08-29 amendment](../decisions/0003-observe-do-not-spawn.md#amendment--2026-08-29-a-human-may-press-send), which overturned a refusal two ADRs had held. **It is not a bet about the mechanism** — tmux delivery into a named pane is proven and tested. It is a bet about **erosion**: that a confirmed, one-shot send removes a trip between two panes without becoming the place prompts are typed. The reason to doubt it is written into the ADR it came from: ADR-0008 predicted the request for a send button before there was one worth wanting, and the next requests are already predictable — *send without confirming*, *send when I flag*, *show me the reply here*. The third is the one that matters, because it is v0.1: a box you type into, that shows you the answer, that you type into again. No harness measures habit, so this is [A37](#ledger)'s shape and takes A37's treatment — built small, with the way back being the deletion of one button and one `send-keys` line. **Removal condition agreed in advance:** a fortnight in which the confirmation is clicked through without being read, or in which a send lands somewhere it was not aimed, or in which the terminal has stopped being where prompts are typed. Any of the three and the button comes out and the clipboard stays, which is where this was on the morning of 2026-08-29 | [ADR-0003's 2026-08-29 amendment](../decisions/0003-observe-do-not-spawn.md#amendment--2026-08-29-a-human-may-press-send) |
| A40 | The commands your **agents** ran are worth completing from, more than your own shell history is | `REFUTED` | The bet under [`R-O12`](roadmap.md#o-a-local-model-beside-the-agents)'s **corpus** half — [A41](#ledger) carries its drafted half, and they can be judged apart. Asked for 2026-08-29. **The competition here is unusually strong and free**: `zsh-autosuggestions`, fish and atuin already complete from shell history, instantly and locally, and a suggestion that is merely *as good* loses on every axis that is not quality — the same trap [A35](#ledger) named for the reading guide. What could make this different is one specific thing: **most of what has actually been run on this machine was run by an agent**, inside a session, and never reached a shell history at all. Whether those commands are the ones you want completed is the bet, and there are two honest reasons to doubt it: an agent's commands are often long, one-shot and path-specific — the *least* re-typable text there is — and the ones worth having again may be exactly the ones you already typed yourself. **The harness comes before the panel**, as it did for `A35`, `A36` and `A38`: replay real commands from the corpus, hide the tail after `k` characters, and measure top-1 and top-5 prediction against the baseline of `history.jsonl` alone. **Removal condition agreed in advance:** the corpus predicting no better than your own shell history, or a fortnight in which the suggestion is not accepted — either way the row comes out rather than being tuned, because the thing it would be tuned against is a tool you already have **REFUTED 2026-08-29, by `--bin judge --complete`, before a single pixel was drawn.** Over **11,359 agent-run commands** and **10,403 shell history lines** on this machine, holding out the last 60 things actually typed: the agent corpus predicted **0 of 57** at a 3-character prefix, **0 of 43** at six and **0 of 29** at twelve, where the shell history a completer already reads for free got **22, 27 and 13**. Not *worse* — **nothing**. The reason is visible the moment the two corpora are put side by side, and it is the doubt this row filed in advance rather than a surprise: **11,043 of the 11,359 agent commands are distinct**, a 97% singleton rate, because they are long, piped and path-specific — `cat /tmp/claude-…/task`, `./scripts/check-docs.sh 2>&1 | tail -25`. Prediction is repetition, and there is none. Meanwhile what a human types into their own shell is `sudo ./install.sh`, `yolo`, `clear`, `cd scripts`: the two populations barely intersect, because the long work is what the agent was asked to do. **The removal condition is honoured rather than argued with** — the corpus-prediction half of `R-O12` comes out. What the harness did **not** test, and what is filed as its own claim rather than smuggled back in: *recall* — typing `check-docs` on purpose to find a command you know an agent ran. That is a different gesture from prediction and needs the fortnight, not a harness | [`R-O12`](roadmap.md#o-a-local-model-beside-the-agents) |
| A41 | A model writing a shell command from a sentence beats typing it, **with a coding agent already open beside you** | `UNTESTED` | The second half of [`R-O12`](roadmap.md#o-a-local-model-beside-the-agents), asked for 2026-08-29: *"what if I just want to ask a quick question in the terminal and let ai write the command?"* **The doubt is not whether a model can write `grep -rn xyz . | sort -k1`** — it can, and so can `gh copilot suggest` and every chat window. It is that mogeung's competition here is **two panes away and already running**: an agent that would not only write the command but run it, read the output and fix it. What distinguishes this row is exactly one thing and it needs saying out loud: **you want the command in your own shell, in your own history, not run inside somebody's session** — which is a real difference and a narrow one. [A37](#ledger) is the precedent and it is the same shape: the incumbent is a chat window, and it is a strong one. **Removal condition agreed in advance:** a fortnight in which drafted rows are read and not accepted, or in which the same question gets asked in the Agent pane anyway — the drafted half comes out and the corpus picker stays, since they can be judged apart **Day one, 2026-08-29:** built, installed and used against a real shell, and wanted — *"I like this feature, quite useful"*, with two refinements asked for from use within the hour (focus following the command, and `Alt+Enter` to run it). Nobody asks for refinements to something they have stopped opening, so this points the right way and is the best evidence available this early. **It is still not the test**, for the reason [A27](#ledger) is in this ledger at all: A27 looked fine on its first day and went `AT RISK` on a count of two notes in a week, and the incumbent here — an agent already running two panes away — is stronger than a text editor was. Stays `UNTESTED` until the fortnight `R-O8` owns has actually passed | [`R-O12`](roadmap.md#o-a-local-model-beside-the-agents) |

## Notes on the most dangerous ones

**A1 and A6 are the product.** If either is false, mogeung has no reason to
exist, and no amount of polish on the review layer compensates. They are also
the cheapest to test: use it for a week. Everything on the roadmap is
speculation until they are resolved.

**A4 is the operational risk.** Everything rests on two undocumented file
layouts. The parser degrades rather than crashing, so the realistic failure is
mogeung quietly seeing *less* than it should — the worst kind, because it looks
like "nothing is happening" rather than an error.

It stays `AT RISK` — instrumentation does not make a private format stable. What
changed is that the failure is now *detectable*: every line is classified, and
an unrecognised type raises a named alert. The instrumentation immediately
earned its place by finding three event types that had been discarded silently
for the whole of v0.2. Nobody had noticed, which is precisely the point.

**A24 and A26 are now coupled, which neither was written expecting.** A24 is a
statement about a daemon that only reads; A26 is the bet that motivates giving
that daemon write verbs. Resolving A26 favourably therefore does not leave A24
where it was — it changes what A24 is an assumption *about*. ADR-0012's
loopback-or-token guard is what keeps the original sentence true, and if that
guard is ever relaxed for convenience, A24 should be re-filed as `AT RISK` the
same day rather than left reading as though nothing moved.

**A37 is A27 with a stronger incumbent, and that is the point of filing it
separately.** A27 bet that writing would happen here rather than in the editor
already open, and it is `AT RISK` on a count of two notes in a week. A37 makes
the same bet about asking rather than writing, against a competitor that is
better at its job than a text editor is at being a notepad. If it survives, the
thing it will have shown is that the *switch* was the cost both times — which
would be worth knowing about A27 too.

**A10 deserves scrutiny.** It was the opening complaint and remains untouched
after two versions. Either it matters and we have been avoiding it, or it
mattered less than stated. Worth deciding honestly rather than drifting.
